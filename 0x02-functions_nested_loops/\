#include "main.h"

/**
 * print_last_digit -> prints last digiit
 * @n: Number
 * Returm: Always return n % 10
 */
int print_last_digit(int n)
{
	int i;

	if (n < 0)
	n = -n;
	i = n % 10;
	_putchar(i + '0');
	_putchar('\n');
	return (n % 10);
}
