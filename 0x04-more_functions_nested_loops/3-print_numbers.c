#include "main.h"

/**
 * print_numbers - prints numbers from0 to 9
 * @n: Parameter print
 * Return: void
 */

void print_numbers(int n)
{
	for (n = 0; n <= 9; n++)
	{
		_putchar(n);
	}
	return (n);
}
