#include <stdio.h>
#include "main.h"

/**
 * binary_to_unit - converts binary numbers to int
 * @b: binary input to be converted
 *
 * Return: the converted number or 0 if ...
 */
unsigned int binary_to_unit(const char *b)
{
	int num;

	if (num < 2)
		return (b);
	else
		return (NULL);
}
