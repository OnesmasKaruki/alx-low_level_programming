#include <stdio.h>

/**
 * main -> main block
 *
 * @ch: parameter
 * Return: Always return 0
 */
int main(void)
{
	char ch = '0';

	while (ch <= '9')
	{
		putchar(c + '0');
		ch++;
	}

	putchar('\n');
	return (0);
}
