#include <stdio.h>

/**
 * main - paramenter passed
 *
 * @n: Parameter
 * Return: Always return n
 */
int main()
{
	int n;

	for (n = 0; n <= 10; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (n);
}
